/* 
block문:
  block statement, compound statemet
  중괄호{}로 묶은 것.
  자바스크립트는 블록문을 하나의 단위로 취급.
  단독으로 사용할 수도 있으나 일반적으로 제어문이나 함수 선언문 등에서 사용.
*/

// 블록문
{
  var a;
  let b;
}

// 제어문(조건문, 반복문....) 
var x = 0;
while (x < 10) {
  x++
}

// 함수 선언문
function sum() {
  x, y;
}